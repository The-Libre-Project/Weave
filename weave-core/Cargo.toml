[package]
name = "weave-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "PE loader and NT API stubs for Weave"

[dependencies]
goblin = { version = "0.9", default-features = false, features = ["pe32", "pe64"] }
