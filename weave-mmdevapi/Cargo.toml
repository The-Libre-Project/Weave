[package]
name = "weave-mmdevapi"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# pipewire will be added here when Step 4 (audio) is implemented.
# It requires pkg-config cross-compilation setup and is not declared
# until real code exists to use it.
