[package]
name = "weave-ipc"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "IPC message protocol for out-of-process guest execution"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"
