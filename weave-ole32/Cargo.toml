[package]
name = "weave-ole32"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ole32.dll / combase.dll stubs for Weave — COM initialisation and object creation"

[dependencies]
weave-common = { path = "../weave-common" }
libc = "0.2"
