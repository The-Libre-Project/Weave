[package]
name = "weave-desktop"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Desktop integration for Weave — .desktop file generation, xdg-mime handler registration, and application menu management."

[dependencies]
goblin = { version = "0.9", default-features = false, features = ["pe32", "pe64"] }
